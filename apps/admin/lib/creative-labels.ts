// MAX-020: shared between the media overview table and the creative
// gallery card — never the bare English enum value (image/video).
export const CREATIVE_TYPE_LABEL: Record<string, string> = {
  image: 'Imagem',
  video: 'Vídeo',
};
