<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- No camera or storage permissions: MAX-007 downloads media into
         app-private storage (no WRITE_EXTERNAL_STORAGE needed). -->

    <!-- MAX-008: only what the pilot's GEO engine needs — precise,
         foreground-only location. No ACCESS_BACKGROUND_LOCATION: the player
         is a continuous foreground experience (item 4 of the MAX-008
         brief), so a Foreground Service covers "the app is running", not a
         background-location grant with its own Play Console review and
         steeper user-trust bar. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- MAX-014: required for PackageInstaller.commit() to succeed at
         all. The *silent* part (no user-facing confirmation dialog) comes
         from this app's Device Owner status, not from this permission
         alone — see kiosk/ApkUpdateManager.kt. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".MaxcarApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaxcarTablet"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.MaxcarTablet">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".work.BootCompletedReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- MAX-014: schedules the rollback-safety worker the instant this
             app itself finishes updating — see work/PackageReplacedReceiver.kt. -->
        <receiver
            android:name=".work.PackageReplacedReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- MAX-014: PackageInstaller.commit()'s own result callback —
             never receives anything except our own explicit-component
             broadcast from kiosk/ApkUpdateManager.kt, so no intent-filter
             and no export. -->
        <receiver
            android:name=".kiosk.ApkInstallResultReceiver"
            android:exported="false" />

        <!-- MAX-011: required for `dpm set-device-owner`. See
             kiosk/AdminReceiver.kt — no policies are actually applied
             through this receiver's callbacks, only through
             DevicePolicyManager calls once Device Owner status is active. -->
        <receiver
            android:name=".kiosk.AdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_receiver" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- MAX-008: keeps the Location Engine (and, by staying alive, the
             player itself) running while the tablet is in operational mode.
             Started only while the player is the active screen — see
             MainActivity.applyKioskMode — never at boot on its own. -->
        <service
            android:name=".geo.LocationForegroundService"
            android:exported="false"
            android:foregroundServiceType="location" />

    </application>

</manifest>
